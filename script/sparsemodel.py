#!/usr/bin/env python

import numpy
import time
import sys
import os

class SparseModel(object):
    '''
    Sparse Model based Energy is code the input image to code vector.
    code vector could be the feature to build model
    '''
    
    def __init__(self, input_data=None, )
